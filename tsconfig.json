{
    "compilerOptions": {
        "module": "CommonJS",
        "target": "es2016",
        "jsx": "preserve",
        // Enable TS type checking in vanilla JS.
        "checkJs": true,
        "allowJs": true,
        // Don't actually emit anything, because this is vanilla JS.
        "noEmit": true
    },
    "include": [
        "src/**/*"
    ],
}